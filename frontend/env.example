# Vite environment variables must be prefixed with VITE_
# Backend base URL used by the frontend for API calls.
# Example for local dev:
# VITE_API_BASE=http://localhost:8000

# Example for production (Railway backend URL):
# VITE_API_BASE=https://your-backend.onrailway.app

# Comma-separated list of allowed hosts for Vite preview (or "*"/true)
# Examples:
# VITE_ALLOWED_HOSTS=open-ai-hay-fe.up.railway.app
# VITE_ALLOWED_HOSTS=open-ai-hay-fe.up.railway.app,another.example.com
# VITE_ALLOWED_HOSTS=*
